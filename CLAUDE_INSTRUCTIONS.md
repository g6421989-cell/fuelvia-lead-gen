═══════════════════════════════════════
READ THIS BEFORE DOING ANYTHING
═══════════════════════════════════════

1. You already know PROJECT_MEMORY.md completely
2. That file contains full project context, 
   file structure, business logic and current status
3. Never make assumptions — everything is documented there

AUTO UPDATE RULE:
Every time you create a file — append to PROJECT_MEMORY.md:
[FILE CREATED] filename — what it does — status

Every time you modify a file — append to PROJECT_MEMORY.md:
[FILE MODIFIED] filename — what changed — status

Every time you complete a feature — append to PROJECT_MEMORY.md:
[FEATURE DONE] feature name — how it works — date

Only append. Never rewrite the whole file.
═══════════════════════════════════════
